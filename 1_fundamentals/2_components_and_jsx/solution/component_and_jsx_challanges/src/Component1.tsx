import React from 'react';

// React component should start with a capital letter so that React can distinguish between HTML tags and React components
export default function Component1() {
  return <div>I'm</div>;
}
